# Baseline — personal-interviewer @ `2dac711` (2026-09-02)

**Loại tài liệu:** fact sheet (Phase 1 của re-baseline review). Không có opinion; mọi đánh giá nằm ở
`docs/plans/final-form-2026-09-02.md` (Phase 2–4, chưa viết).

**Quy ước tag:** `[MEASURED]` = có artifact/test/audit/CI, cite path · `[INFERRED]` = suy từ code, cite
`file:line` · `[OPINION]` = không dùng trong tài liệu này. Số không có tag = lỗi của tài liệu.

**Ràng buộc khi lập:** read-only; không chạy `coach bench` live; không chạy `pytest -m live` / `-m rag`;
chỉ đọc artifact bench đã check-in. Đọc theo đúng thứ tự CLAUDE.md → CONTEXT.md → ADR 0001–0014 (lọc
`Status:` theo section) → issues 0001–0037 (`## Status`) → `docs/audits/*` → GH issues/PRs → code +
tests → `git log`. Phần inventory code (mục 2, 6) có dùng 4 subagent đọc song song; mọi claim
file:line quan trọng (rubric anchors, `question_node` exception net, judge pin, MiMo default, replay
artifact keys, ledger rows) đã được kiểm tra lại trực tiếp.

---

## 1. Anchor

| Mục | Giá trị | Tag |
| --- | --- | --- |
| Commit | `2dac711b3b9efc292406222e369fcd0a45301872` — "fix: toneless-Vietnamese detection stops reading English as Vietnamese (#120)" | `[MEASURED]` `git rev-parse HEAD` |
| Branch / tree | `main`, working tree clean, remote `nekloyh/get-hired` | `[MEASURED]` `git status` |
| Ngày commit cuối | 2026-08-04 09:42 +0700 — **29 ngày** trước baseline này | `[MEASURED]` `git log -1` |
| Tổng commit / tác giả | 90 commits; 2 identity (nekloyh 46, "Nguyễn Lý Minh Kỳ" 44) | `[MEASURED]` `git log` |
| Nhịp commit | 07-07: 22 · 07-11: 10 · 07-19: 7 · 07-20: 1 · 07-27: 14 · 08-02: 5 · 08-04: 1 — làm theo "wave", mỗi wave 1–2 ngày | `[MEASURED]` `git log --date=short` |
| `uv run pytest -q` | **805 passed, 2 xfailed, 9 deselected**, 1 warning, 8.31s (816 collected) | `[MEASURED]` chạy tại chỗ 2026-09-02 |
| 2 xfailed | `tests/test_language.py::test_a_vietnamese_name_in_english_prose_is_a_known_gap` ×2 params, `strict=True`, reason "R-23 residual" | `[MEASURED]` `pytest -rx` |
| 9 deselected | 7 `live` (evaluator 1, forge 1, interviewer 1, microloop 3, supervisor 1) + 2 `rag` (concepts, resources); `addopts = "-m 'not live and not rag'"` | `[MEASURED]` `pytest --collect-only -m "live or rag"`; `pyproject.toml:47` |
| `uv run mypy src` | Success: no issues found in 33 source files | `[MEASURED]` chạy tại chỗ |
| ruff | CI job `python` chạy `ruff check` và xanh trên HEAD (không chạy lại tại chỗ) | `[MEASURED]` `.github/workflows/ci.yml:15`, GH run 2026-08-04T02:42:54Z |
| CI trên HEAD | `CI` completed **success** 2026-08-04T02:42:54Z; 2 jobs `python` (uv sync --dev, ruff, mypy, pytest -q) + `web` (npm ci, lint, test, build). **Không** có e2e, Docker build, bench, replay, hay LLM live trong CI | `[MEASURED]` `gh run list`; `.github/workflows/ci.yml` |
| Tags | không có tag nào | `[MEASURED]` `git tag -l` |
| Open PRs | #104 **draft** (anchor depth/system_thinking, GH #96; 1 commit, cập nhật cuối 2026-07-27; tự ghi "DO NOT MERGE until 2 more green invocations") · #50 (frontend redesign, không draft, chưa rebase) | `[MEASURED]` `gh pr list` |
| Open issues | 15: **blocker** #59 · **high** #71, #72, #83, #84, #86, #119 · **medium** #60, #79, #85, #87, #88, #96, #103, #113 | `[MEASURED]` `gh issue list --state open` |
| #78 | CLOSED 2026-08-04 bởi #120 (commit HEAD) — CLAUDE.md và memory vẫn nhắc như đang mở | `[MEASURED]` `gh issue view 78` |
| Nhánh local chưa dọn | `spike/0078-*`, `spike/0079-*`, `fix/0065-*`, `fix/0067-*`, `fix/0078-*`, `feat/0070-*`, `feat/0080-*` (đã merge, còn trên remote) + 15 nhánh `worktree-wf_*` chỉ local | `[MEASURED]` `git branch -a` |

**Đề xuất tag (chỉ đề xuất, chưa tạo):** `base/2026-09-02` tại `2dac711`.

---

## 2. Inventory hệ thống

Cột "LLM call" ghi role theo ADR 0010 (`judge` / `interviewer` / `supervisor` / `diagnostic` / `planner`).
Cột "Tests" đếm `def test_` (parametrize mở rộng thêm khi chạy). Tất cả `[INFERRED]` trừ khi ghi khác.

| Component | Entry point | Trách nhiệm | LLM call bên trong | Tests |
| --- | --- | --- | --- | --- |
| **Diagnostic → Topic Plan + priors** | `diagnostic.py` — `diagnose` (:203), `diagnose_or_degrade` (:213-242), `_diagnose_topic_plan` (:250-279) | Từ profile → Topic Plan (ordered Skill × target difficulty × rationale) + Beta priors: baseline 0.5, self-claim `0.2+(v−1)·0.15`, ledger mean override, clamp [0.05, 0.95]; Role criticality (3 role string + 3 company) chỉ đổi prior *strength* (:375-385); correlations áp **một lần** (:354-362) | 1 × `chat_json(DiagnosticPlanResponse)` role `diagnostic`; `client=None` hoặc lỗi → deterministic `_ordered_skills` (:388-396) | `test_diagnostic.py` 11 |
| **Supervisor (macro-loop)** | `supervisor.py` — graph `run_question`→`supervisor`→`study_plan` (:328-339), `decide_next_move` (:342-400), `question_node` (:233-308) | Plan-executor: rails deterministic **trước** LLM — `max_questions` 5 (:56), `max_elapsed_seconds` 1800 (:57), plan exhausted (:365-369), seed availability (:645-649, :574-591); 1 LLM "deviate?" call trả `SupervisorDecision{action, reasoning, target_skill, target_plan_index, will_probe_skill}` (:95-106); validator structured `will_probe_skill` (:523-571, thay phrase-list ở #107); fallback deterministic trên `StructuredOutputError` và mọi `Exception` (:377-400). Checkpointer `SqliteSaver` (:19, :210) | 1 × `chat_json(SupervisorDecision, max_retries=1)` role `supervisor` (:376) | `test_supervisor.py` 50 (1 live), `test_trajectories.py` 2 |
| **Micro-loop** | `microloop.py` — `run_micro_loop` (:189-321) | Interviewer hỏi → Candidate trả lời → Evaluator chấm → nếu `follow_up_recommended` và chưa hết `DEFAULT_MAX_TURNS=4` (:36) → Follow-up. `StopReason` = RESOLVED / SAFETY_CAP / FOLLOW_UP_UNAVAILABLE / FAILED (:127-138). **Last-turn-wins**: `resolved = turns[-1].evaluation` (:316), docstring "keeps the last, not the best" (:189-192). `TurnTrace.llm_calls_by_provider` lấy từ `call_counts` (:233-249) | gián tiếp (Evaluator + Interviewer) | `test_microloop.py` 29 (3 live) |
| **Interviewer** | `interviewer.py` — `_generate_follow_up_native` (:460-511), `render_seed_question` (:647-696) | Agent duy nhất có tool: `lookup_concept` (:263-289). Native tool-calling, `tool_choice` forced, `disable_thinking=True` (:423-425); retry-once trên `UnknownToolCall` (`_NATIVE_TOOL_ATTEMPTS=2`, :333) rồi `FollowUpUnavailable` (:507-511); không có tool call → `ToolCallingUnsupported` (:436-440). Validators: `reject_reask`, `require_grounding`, `require_vietnamese` (chỉ `vn`, :247-259). Render seed sang VN cho vn/mixed, degrade về EN (:683-694) | 1–2 × `chat_with_tools` (follow-up) + 1 × `chat_json(RenderedSeedQuestion)` (vn/mixed) role `interviewer` | `test_interviewer.py` 17 (1 live) |
| **Evaluator (judge duy nhất)** | `evaluator.py` — `evaluate` (:875-966), `_evaluate_once` (:805-835) | Chấm 1–5 mỗi dimension + `weighted_score`, `confidence`, `follow_up_recommended`, evidence quotes. Strict `json_schema` từ rubric active (:611-655). Cross-check `linear_weighted_score` tolerance 1.0 (:742), diverge → confidence cap 0.4 (:749). Triggers `_self_critique_triggers` (:838-844): `confidence < 0.5` (:572) hoặc divergence. Escalation → **panel** (Skeptic + Advocate `_panel_opinion` :658-684, rồi verdict re-read :702-732), `PanelBudget` 1/question (:446-453). Haircuts (evidence-degrade ceiling 1.0→0.4 :364-389; noise 0.85 :397-426) chạy **sau** trigger (:901-903, cố ý). Không còn self-critique re-read độc lập; `SelfCritiqueTrace` giữ cho checkpoint cũ (:103-113). **Không** inject concept note (E3 chưa có) | 1 × `chat_json(Evaluation, max_retries=2)` (+1 evidence-degrade pass, +2 panel +1 verdict khi escalate) role `judge` | `test_evaluator.py` 59 (1 live) |
| **Rubric** | `rubric.py` — `DIMENSION_GUIDE` (:40-86), `Rubric` (:89-115) | Anchor có trong guide: `correctness` 1,2,3,4,5 · `depth` **1,2,4,5** · `communication` **1,3,4,5** · `system_thinking` **1,2,4,5** · `mlops_awareness` **1,5** · `english_delivery` 1,2,3,4,5. Weight 0 tắt dimension; `english_delivery` không thuộc `TECHNICAL_DIMENSIONS` (:25), không vào `weighted_score` | — | `test_bench.py` (anchor-sync test, `KNOWN_SCALE_GAPS`) |
| **Skill state (Beta)** | `skill.py` — `SkillState.observe` (:142-157), `apply_evaluation` (:173-183), `evidence_weight_for` (:160-170) | `EVIDENCE_WEIGHT=2.0` (:30); `confidence_weight(c)=2·(0.25+0.75c)` ∈ [0.5, 2.0] (:75-88); `panel_agreement_weight(d)` ∈ [0.5, 2.0] (:60-72); `score_to_quality=(s−1)/4` (:55); `POSTMORTEM_WEIGHT_RATIO=0.5` (:43). **`difficulty` không được dùng** trong update (chỉ `seeds.py:117-121`) | — | `test_skill.py` 26 |
| **Skill ledger (cross-session)** | `ledger.py` — `load_priors` (:88-132), `load_states`, `save_posteriors` (:182-242) | 1 file JSON `candidate_id → {completed_at, skills{α,β}}`; decay `exp(−ln2·days/30)` về Beta(1,1) (:57-76, `LEDGER_HALF_LIFE_DAYS=30` :42); save atomic: `threading.Lock` (:54) + tmp + fsync + `os.replace` (:221-235), không bao giờ raise (:236-242). Reader không lock; comment "Multi-process … needs real locking" (:49-50) | — | `test_ledger.py` 28 |
| **LLMRouter + providers** | `llm.py` — `_OpenAICompatibleClient` (:363), `MimoClient` (:594), `GroqClient` (:619), `OpenAIClient` (:630), `ZenMuxClient` (:645), `LLMRouter` (:664) | `openai.OpenAI(max_retries=0)` (:392-398); transport loop 4 attempts, backoff 2/5/10s, Retry-After ≤30s (:38-40, :425-462). `insufficient_quota` = substring match (:46-48) → **terminal**, ghi ledger latch, re-raise `openai.RateLimitError` gốc (:430-449). `is_provider_failure` = `openai.APIError` hoặc `EmptyCompletionError` (:127-138); `StructuredOutputError`, `ToolCallingUnsupported`, `TypeError`, `LLMConfigurationError` **không** failover (:132-136, :875-876). Breaker: threshold 3, cooldown 60s, in-memory per router (:158-175). Đúng **một** fallback theo thứ tự `("groq","mimo","openai","zenmux")` (`config.py:158-167`). Trace `llm-call provider= model= ms= prompt= completion= outcome=` (:182, :469-488) | — | `test_llm.py` 46 |
| **Per-role routing + judge pin** | `config.py` — `RoleName` (:23), `ROLE_<ROLE>_PROVIDER/MODEL/TEMPERATURE` (:134-148), `role_config` (:206-219); `llm.py` — `build_role_clients` (:960-978), `_pinned_role_client` (:948-956) | Judge = concrete client, **không** router wrapper (:978); `_require_bench_validated_judge`, `BENCH_VALIDATED_JUDGE_PROVIDERS={"openai"}` (`config.py:36`, :222-244); opt-out `COACH_ALLOW_UNVALIDATED_JUDGE` (:99). Role không set → `primary_provider` + model của provider + `LLM_TEMPERATURE` 0.2 toàn cục (:101, :206-219) — **không có bảng default per-role**; ví dụ hybrid chỉ nằm comment `.env.example:62-69`. Judge pin có nhánh thoát: `RoleClients.single(default)` khi `settings` không có `role_config` (`llm.py:970-974`) | — | `test_roles.py` 21 |
| **Usage ledger + budget** | `usage.py` — `budget_stop_reason` (:541-593), `start_refusal_reason` (:501-518), `session_budget_guard` (:596-628) | JSONL `logs/usage-ledger.jsonl` (:48, env `COACH_USAGE_LEDGER`); daily budget 2.5M (:59); per-run budget = `worst_case_calls × 2,700` (:107, :166-181) — mặc định 5q×4t → 284 calls → 766,800 tokens; sizing 1,000 setup + 5,200/question (:108-109) từ "measured inputs" 26,866 / 2,663 / 993 (comment :94-101); product cap 480 questions/ngày (:115) **web-only** (`web_api.py:774`). Suspend là `SessionBudgetSuspended` raise **ở driver, ngoài graph** (:202-209; `cli.py:404-406`, `web_api.py:980-981`) tại ranh giới node | — | `test_usage.py` 47 |
| **Bench (`coach bench`)** | `bench.py` — `run_bench` (:216), median (:205-207), straddle (:98-109); `cli.py:757-812` | 35 cases `data/bench/cases.yaml` (ml_fund 12, dl 7, sd 6, mlops 6, vnlp 4; 18 en / 12 vi / 5 mixed; 12 cặp EN/VN đầy đủ + 11 đơn). `BENCH_DEFAULT_K=3` (:67); errored draws bị bỏ nếu ≥1 sống (:192-213); gate = mọi case `within_band` + `delivery_within_band` (:233-242); straddle chỉ warn (`cli.py:808-811`); `BIAS_MIN_SAMPLES=8`, `BIAS_TRIPWIRE=0.5` (:266-271); preflight 70k × k tokens (`cli.py:738`). Chạy trên `roles.judge` (`cli.py:761-763`). **Không** chạy trong CI | judge, 35 × k calls (+panel nếu escalate) | `test_bench.py` 37 |
| **eval_harness** | `eval_harness.py` (:73-123); `coach eval-harness` | 5 golden hard-coded, single-run, exit-code. Còn dùng làm **gate 3 của Forge** (`forge.py:36, 425-445`) | judge | `test_eval_harness.py` 4 |
| **Replay bench** | `replay.py` — `run_persona_session`, `ReplayArtifact` (:148-155, version 1 :38), `replay_decision` (:184-195) | Persona → Session không giám sát → metrics `posterior_masteries`, `probed_ordering` vs ground truth, `attempts_by_skill` (:61-63, :126-145). Artifact **không có field pack** (#113); `replay_decision` gọi `decide_next_move(client, final_state, now)` không bank (:195). **Không có CLI command**; chỉ tests + `scripts/dump_serde_goldens.py`. 1 artifact `data/replay/deep-learning-strong.json` (gpt-4o-mini, commit `62609fa`, 2026-07-07). **Không có** code hai arm E1 | supervisor (khi replay decision) | `test_replay.py` 4 |
| **Retrieval eval set** | `retrieval_eval.py` (thư viện) + `scripts/retrieval_eval.py` (CLI, exit 0/1/2) | Labels frozen `data/bench/retrieval-labels.yaml` v1, **62 cases** (59 seed + 3 live; 7 vi), sha256 pinned (`tests/test_retrieval_eval.py:38,113`); hit@1 + Wilson 95% CI (:263-279); `TARGET_SET_SIZE=150` (:41); harvest live queries từ export `.md`/JSON (:358-429), in stub YAML ra stdout (:458-486). Không phải `coach` subcommand | — | `test_retrieval_eval.py` 59 |
| **Forge** | `forge.py` — `run_forge` (:459-523); `coach forge` | Writer → gate contract (`bank.validate_question`) → novelty Jaccard 0.6 (:54) → admission qua `eval_harness` (strong ≥3.5, weak ≤3.0; :59-60). Ghi review queue `data/forge/review-queue-<date>.yaml`, **không bao giờ** sửa bank (:17-20, test `:413`). Queue hiện tại: 1 file 2026-07-11, 3 entries, header "MERGED 2026-07-19" — không có gì pending | judge (writer + admission) | `test_forge.py` 31 (1 live) |
| **Postmortem** | `postmortem.py`; `coach postmortem` | 5–8 câu elicitation (budget do Python giữ, :50-51, :231-244) + 1 reconstruction (:255-260) + `plan_study` best-effort; fuse vào ledger đã decay với weight `0.5 × confidence_weight` (:277-280); abort → exit 2, ledger byte-identical | judge/planner | `test_postmortem.py` 14 |
| **Export & TurnTrace** | `exporter.py` — `render_session_markdown` (:32-49); `microloop.py:142-164` (`TurnTrace`) | Markdown duy nhất: Summary → Final Skill States → "Since Previous Session" (chỉ khi có `ledger_prior_mastery`, :68) → Topic Plan → Transcript (per turn: dimension table, triggers, concept lookup, `LLM calls: N (provider n)` :148-153, delivery fixes, panel packet) → Supervisor Decisions → Study Plan. Web persist `data/exports/<safe-id>.md` khi `complete` (`web_api.py:944-951`), **không có retention** cho exports (checkpoint reaper 7 ngày, `config.py:130`) | — | `test_study_planner.py` 12, `test_web_api.py` (export tests) |
| **session_serde + goldens** | `session_serde.py`; `tests/test_serde_golden.py`; `scripts/dump_serde_goldens.py` | Views `TraceRecord`/`TurnRecord`/`TranscriptItem`/`DecisionRecord`. Goldens (6 file, `tests/golden/`): replay-trajectory.json, supervisor-prompt.txt, session-export.md, session-summary.txt, skill-state-rows.txt, study-plan-prompts.txt — byte identity (:62-69). **Không** cover raw SqliteSaver bytes và web payloads. Không có version constant ngoài `REPLAY_ARTIFACT_VERSION=1` | — | `test_session_serde.py` 10, `test_serde_golden.py` 2×6 |
| **Language (ADR 0007)** | `language.py` — `answer_is_english` (:128-154), `rubric_with_delivery` (:169-182) | Modes `en|vn|mixed`; detector: VN-char ratio > 0.03 (:42) → không EN; function-word gate **đếm distinct** (`hits=len({...})` :151), `_VN_WORD_MIN_HITS=2`, ratio ≥ 0.08 (:120-121); list 48 từ (:63-118) **còn `la` (:81), `du` (:102)**, đã bỏ `vi`, `em` (:53-55); comment "NOT collision-free" (:49-55). Delivery active iff mode≠vn ∧ ≥5 từ ∧ EN | — | `test_language.py` 25 (+2 strict xfail) |
| **Concepts / RAG** | `concepts.py` — `InMemoryConceptStore` (:106-145), `ChromaConceptStore` (:148-261), `lookup_concept` (:374-383) | `concept_store="auto"` (`config.py:125`) → Chroma nếu import được, else memory + WARNING (:323-331). Embedder theo language: `multilingual-e5-small` cho vn/mixed, `bge-small-en-v1.5` cho en (:294-306), persist dir namespaced per embedder (:309-320), e5 prefix `query:/passage:` (:32-34). Shelf 40 notes (`src/interview_coach/data/concepts.yaml`: 34 en / 6 vi). Web demo mode ép `memory` (`web_api.py:805-810`). **Docker image không cài rag extras** (`Dockerfile:27-42`) | — | `test_concepts.py` 18 (1 rag) |
| **Resources (Study Planner catalog)** | `resources.py` — `SEED_RESOURCES` 10 entries (:250-381) | Web luôn dùng `memory` (`web_api.py:811`); Chroma store BGE-only (:147-157) | — | `test_resources.py` 4 (1 rag) |
| **Bank + packs (ADR 0008)** | `bank.py` — `validate_question` (:138-209), packs (:245-270); `seeds.py` — `select_seed_question` (:92-126), `rotation_offset` (:79-89); `coach pack lint` (`cli.py:700-712`) | Bank built-in **45 câu** (ml_fund 9, dl 9, mlops **12**, sd 9, vnlp 6; difficulty 1:5 · 2:10 · 3:13 · 4:11 · 5:6) + 40 notes, nằm **trong package** (`src/interview_coach/data/`). Pack duy nhất `data/packs/fpt` (20 câu = 4/Skill, 10 notes). Chọn seed theo `|difficulty − target|` rồi rotation `sha256(session_id) mod n` (per Session, không per ledger) | — | `test_bank.py` 17, `test_pack.py` 10, `test_seeds.py` 9 |
| **Study Planner** | `study_planner.py` — `plan_study` (:169-199) | 1 LLM call, validators (:298-334); input **chỉ** `session_state` hiện tại (:205-208, :428-454), **không** đọc ledger/history; lỗi → `study_plan_error` ở `supervisor.study_plan_node` (:314-325) | 1 × `chat_json(StudyPlanDraft)` role `planner` | `test_study_planner.py` 12 |
| **Web API** | `web_api.py` — WS `/api/sessions/{id}` (:543-628), `GET /api/health` (:525-541), `GET /api/sessions/{id}/export.md` (:630-653) | Client→server: `auth`, `start_session`, `resume_session`, `candidate_answer`, `cancel_session`; server→client: `session_started`, `question`, `state_update`, `session_completed`, `session_error`. Auth: `COACH_AUTH_TOKEN` (`config.py:107`), auth frame ≤10s (:122, :200-210), constant-time compare (:171-182); Origin check trước `accept()` (:545-551, :134-168); session id do browser mint (`web/src/lib/sessionId.ts:134-143`), 2 socket cùng id bị từ chối (:557-564). Cancel = sentinel `_CANCEL` (:213-243). Reaper checkpoint chỉ lúc dựng app (:427-468, :662-672). Single-worker guard (:353-384). **Budget suspend gửi dạng `session_error` "Session suspended: …"** (:891-896), không có message type riêng. Demo mode = `DemoLLMClient` khi `settings.configured` false (:732-747) | gián tiếp (graph) | `test_web_api.py` 86 |
| **Frontend** | `web/src` — `App.tsx` (setup/workspace/report), `SetupPanel`, `SessionAlert`, `SkillBars`, `TopicPlan`, `ReportView` | Vite 7.3 + React 19.2 + TS 5.9. `SKILLS` hardcode 5 Skill (`web/src/lib/types.ts:1-7`). i18n **absent** (chrome EN, `lang="en"`); session history / dashboard (0035) **absent**; PR #50 redesign **không trên main** (font Inter). Vitest 25; Playwright 2 spec (`demo-flow`, `reconnect-flow`), chromium + mobile-chrome, không chạy trong CI | — | vitest 25 |
| **CLI** | `cli.py` — `main` (:960-1330) | Subcommands: `evaluate`, `interview`, `diagnose`, `session` (`--resume/--scripted/--pack/--candidate/--language/--concept-store/--export-markdown/--diagram`), `postmortem`, `eval-harness`, `usage`, `bench --k`, `forge`, `pack lint`, `ingest-concepts`, `ingest-resources`, `api`. `RoleClients` qua `build_role_clients` (:1363-1367). CLI **không** dùng `DemoLLMClient` | — | `test_cli.py` 38 |
| **Docker / deploy** | `Dockerfile`, `docker-compose.yml`, `deploy/nginx/conf.d/coach.conf`, `docs/deploy.md` | 2 stage (node:22 build web dist; uv python3.12 `--frozen --no-dev`, **no rag extras**); non-root uid 10001; state `/app/state`; healthcheck `/api/health`. Compose: `app` (không publish port) + nginx 1.27 TLS/wss, Origin forward verbatim, timeout 3600s. Certs local self-signed untracked | — | không có test |
| **Coaching memory (ADR 0006 addendum)** | — | **Chưa có consumer**: Study Planner không đọc history; web không có dashboard; export chỉ có delta "Since Previous Session" từ `ledger_prior_mastery` (`exporter.py:68`). Slice 0035 / GH #83 Open | — | — |
| **Telemetry** | `telemetry.py` (:18-37) | Counters in-memory `llm.calls`, `llm.calls.<provider>`, transport/noise counters; reset autouse trong tests (`conftest.py:41-59`) | — | `test_usage.py` (telemetry tests) |

**Số LLM call một Session (mặc định 5 câu × ≤4 turn)** `[INFERRED]` `usage.py:107-109, :130-181`, `tests/test_usage.py:199-202`: worst-case 284 calls / 766,800 tokens; sizing kế toán 1,000 tokens setup + 5,200 tokens/question. Không có audit nào đo token/Session thực tế end-to-end (chỉ có bench-run tokens, xem mục 5).

---

## 3. Bảng ADR

12 file ADR: 0001–0011 và 0014. **0012, 0013 không tồn tại** — ADR 0014 ghi rõ "intentionally reserved" cho E3/E5 nếu tách thành ADR riêng `[MEASURED]` `docs/adr/0014:4-7`.

| ADR | Status (theo section) | Implemented ở đâu | Evidence | Drift so với code |
| --- | --- | --- | --- | --- |
| **0001** Control hierarchy | Body **Accepted**. Amendment 2026-07-19 **Proposed** (gate E1, E4) | `supervisor.py` (graph :328-339; rails :351-369 trước LLM; 1 call :376; fallback :466-492); `microloop.py`; `evaluator.py` là judge duy nhất; Interviewer không chấm | `test_supervisor.py` 50, `test_microloop.py` 29, live `test_supervisor.py:725` `[MEASURED]`; audit 07-27 shadow escalations 0 tại <0.5/<0.6/<0.7 `[MEASURED]` | (1) Premise "self_critique has an obvious deterministic trigger" — trigger dormant (ADR tự ghi trong section Proposed). (2) Self-critique re-read độc lập **không còn tồn tại** trong code: escalation → panel Skeptic/Advocate + verdict (`evaluator.py:911-936`); CONTEXT.md vẫn định nghĩa Self-critique là "re-evaluation the Evaluator triggers on itself". (3) Amendment cite "`supervisor.py:569` phrase-list validator" — đã thay bằng structured `will_probe_skill` (`supervisor.py:523-571`, #107); line ref stale. (4) E1 chưa chạy, không có code hai arm |
| **0002** Beta skill state, prior-only correlations | Body **Accepted**. Addendum evidence semantics 2026-07-19 **Proposed** (gate E2) | `skill.py` (`observe` :142-157, `EVIDENCE_WEIGHT` :30); `diagnostic.py` correlations một lần (:354-362), criticality → prior strength (:375-385), không đổi mean (:334-372) | `test_skill.py` 26, `test_diagnostic.py` 11 `[MEASURED]`; `difficulty` grep-empty trong skill/microloop/evaluator `[INFERRED]` | (1) Body: không drift. (2) Addendum: E2 có **một** datapoint ad-hoc n=1 (GH #79 comment 2026-08-01: |err| 0.18514 → 0.24257, **+31.0%**, tức "lose" theo tiêu chí ≥10% improve) — ADR viết "Either outcome is recorded here with the numbers" nhưng **ADR chưa cập nhật**, branch `worktree-wf_d3a73c05-c4f-4@45ad539` chưa push `[MEASURED]` GH #79. (3) Cite `microloop.py:284` → nay `:316`. (4) `skill.py:8-11` docstring nói correlations/priors "out of scope" — stale so với `diagnostic.py` |
| **0003** Tools chỉ ở Interviewer | **Accepted** + 3 addenda (native tools; resilience; 2026-07-19 rationale rewrite) — tất cả binding | `interviewer.py` (tool :263-289; native :374-511; retry-once :333, :489-511); `llm.py` `chat_with_tools` (:851-887; `ToolCallingUnsupported` propagate :875-876); `microloop.py:283-295`; planner degrade `supervisor.py:314-325` | `test_interviewer.py` 17, `test_llm.py` tool tests (:540-565) `[MEASURED]`; audit `0007-rag-interviewer-smoke.md` (MiMo-era) | (1) Comment rationale MiMo còn trong `interviewer.py:10-12, :34`; `disable_thinking=True` vẫn truyền (:425); `MimoClient` còn (`llm.py:594-616`). (2) E3 (inject `expected_concepts` vào judge) **chưa có code** (grep `evaluator.py` rỗng) — đúng với gate |
| **0004** Hand-roll trước LangGraph | **Executed — historical** | `StateGraph` + `SqliteSaver` (`supervisor.py:19, :210, :339`) | — | Không có gì để tuân thủ. Pointer "whatever replaces SqliteSaver" → 0036/#84 Open |
| **0005** Intent aborts / infra degrades | **Accepted** + addendum budget exhaustion 2026-07-19 (binding; R-25 #80 → PR #118 merged) | `CandidateIntent` (`microloop.py:39-57`); `question_node` re-raise :270-276 rồi `except Exception` :277-297 → `TranscriptItem.failed`; web `_CANCEL` (`web_api.py:213-243`); budget: `usage.py:202-209, :501-593`; drivers `cli.py:404-406`, `web_api.py:980-981` | `test_web_api.py` (budget/suspend/resume), `test_usage.py` 47, `test_cli.py` `[MEASURED]`; GH #119 đo trên main: `rc=2 status=active stop_reasons=['failed']` `[MEASURED]` #119 body | **#119**: `insufficient_quota` raise **trong** `question_node` bị `except Exception` nuốt → ghi **1** `failed` zero-evidence rồi mới suspend ở node boundary; `llm.py:442-447` tự ghi nhận. Vi phạm đúng câu "never `failed`-and-advance" của addendum |
| **0006** Memory = decayed priors | **Accepted** + addendum coaching memory carve-out (binding) | `ledger.py` (decay :57-76; half-life 30d :42; atomic save :182-242); `diagnostic.py:346-350`; `exporter.py:68` ("Since Previous Session") | `test_ledger.py` 28 (`:408-431` fresh evidence dominates) `[MEASURED]` | (1) Addendum: "prompt-construction unit tests assert that no prior-session transcript text enters the message lists" — **không có test nào như vậy** (grep tests; chỉ docstring `ledger.py:7`). (2) Consumer coaching memory (0035/#83) chưa có: planner không đọc history, web không có dashboard |
| **0007** Language first-class | **Accepted** (không addendum) | `language.py`; `rubric.py:25, :121-124`; `evaluator.py:517-562`; `interviewer.py:247-259, :647-696`; `study_planner.py:270-276` | `test_language.py` 25 + 2 strict xfail; bench mixed 5/5 in band, `mixed_ml_leakage_broken_english` technical 4.00 / delivery 2 = label (audit 07-27) `[MEASURED]` | (1) "paired EN/VN golden answers … regression proof that the same technical content scores the same" — đo được max |Δ| **2.00** trên `panel_sd_retry_storm` (VN 3.00 vs EN 1.00) dù in band (audit 07-27). (2) Toneless detector: 2 strict xfail (tên riêng VN), `la`/`du` còn trong list |
| **0008** Packs là data contract | **Accepted** | `bank.py:138-270`; `seeds.py:79-126`; `coach pack lint` (`cli.py:700-712`); `data/packs/fpt` | `test_pack.py` 10, `test_bank.py` 17 `[MEASURED]` | (1) "engine ships content-free" — bank 45 câu + 40 notes nằm **trong package** `src/interview_coach/data/`; `SEED_QUESTIONS` = list ml_fundamentals (`seeds.py:76`). (2) Nửa còn lại (Skill/criticality/correlations) hardcode — ADR 0014 Proposed thừa nhận. (3) Replay bench không đo được pack (#113) |
| **0009** Bench gates judge | **Accepted** + (a)(b)(c) 2026-07-19 + (d) 2026-07-27 — tất cả binding | `bench.py` (k=3 :67; median :205-207; straddle :98-109; errored :192-213; bias :266-284); `cli.py:757-812` (preflight 70k×k); judge pin `llm.py:948-956, :978`; `config.py:36, :222-244`; typed failover `llm.py:127-138` | Audit 07-27: **35/35**, 4 invocations × k=3 = 12 sweeps, 0 straddle `[MEASURED]`; `test_bench.py` 37, `test_roles.py` 21 `[MEASURED]` | (1) Line refs stale: "`llm.py:392`" nay là constructor `OpenAI(`; "`config.py:69`" là default `primary_provider`; order ở `config.py:164`. (2) (d) "~150k for k=3 over 35 cases" vs đo 192,136 (audit 07-27) và preflight code 210k (`cli.py:735-738`). (3) (b) "replayed live-session answers … human-reviewed before admission" — **không có code path** harvest answer vào `cases.yaml` (chỉ có harvest retrieval). (4) (c) "replay bench" không có CLI, artifact duy nhất 2026-07-07 (gpt-4o-mini). (5) `forge.py:566-568` report text "a mid-run provider failover silently swaps the judge" — stale sau pin. (6) Addenda (e)(f) trong PR #104 **chỉ trên nhánh PR**, chưa vào main. (7) Escape hatch `COACH_ALLOW_UNVALIDATED_JUDGE` (`config.py:99`) và nhánh `RoleClients.single` (`llm.py:970-974`) — ADR không nhắc |
| **0010** Per-role routing | **Accepted (2026-07-19)**; implemented #95 | `config.py:23, :134-148, :206-219`; `llm.py:960-978` | `test_roles.py` 21; golden `tests/golden/supervisor-prompt.txt` `[MEASURED]` | (1) "Each role maps to its own provider/model **and decoding**" + hybrid $87/1000 sessions — code **không có bảng default per-role**; unset → primary + `LLM_TEMPERATURE` toàn cục 0.2; hybrid chỉ là comment `.env.example:62-69`. Tức ADR mô tả *khả năng*, default vẫn là single-config. (2) "$87 vs $176" là ước lượng pricing trong doc, không có artifact repo. (3) Cite `config.py:56` → nay `:101` |
| **0011** Derived confidence | **Proposed** (gate E4, E5) — không áp vào code | — | Dead signals ADR cite được xác nhận lại 07-27: confidence [0.9,1.0] n=34 (mean 0.94), [0.7,0.9] n=1 (0.89); shadow escalations 0 `[MEASURED]` audit 07-27 | Không drift (chưa implement là đúng). ADR viết "over the 29 cases" — bench nay 35. R-16 (#71), R-17 (#72) Open. E4/E5 chưa chạy |
| **0014** Taxonomy as pack data | **Proposed** (gate E6) — không áp vào code | — | `SKILLS` hardcode `diagnostic.py:22-28`, `web/src/lib/types.ts:1-7`; role table 3 role / 3 company `diagnostic.py:118-146`, unknown role → all PERIPHERAL (:323) `[INFERRED]` | Không drift. Seam bank-parameterized đã có ở Supervisor rails (#114). E6 chưa chạy; pack duy nhất (`fpt`) dùng đúng 5 Skill cũ |

### Experiment gates E1–E6

| Exp | Gate của | Đã chạy? | Artifact | Verdict |
| --- | --- | --- | --- | --- |
| **E1** LLM deviation vs policy-only (3 persona × 3 rotation × 2 arm, replay bench) | ADR 0001 amendment | **Chưa** | Không có code arm (`grep policy_only/arm` src+scripts+tests rỗng), không có audit | — |
| **E2** Evidence semantics (difficulty term, fold-every-turn, derived-confidence input) | ADR 0002 addendum | **Một phần, ad-hoc**, n=1 | GH #79 comment 2026-08-01 (không có trong `docs/audits`); branch chưa push | Post-R-24 |err| **+31.0%** trên `data/replay/deep-learning-strong.json` (mlops GT 0.2) → **lose** theo tiêu chí; tác giả tự ghi "n=1 … should not settle the question alone" `[MEASURED]` #79 |
| **E3** Judge grounding bằng inject `expected_concepts` | ADR 0003 addendum | **Chưa** | Không có code trong `evaluator.py` | — |
| **E4** K=3 cheap votes → derived confidence calibration | ADR 0011 | **Chưa** | Không có code/audit | — |
| **E5** Shadow cutover 100 sessions | ADR 0011 | **Chưa** | — | — |
| **E6** Pack thứ hai với Skill mới | ADR 0014 | **Chưa** | Chỉ có pack `fpt` (5 Skill cũ) | — |
| *(tiền thân)* Forced-escalation 10 case | issue 0027 | **Đã** 2026-07-11 | `scripts/experiment_issue_0027_forced_escalation.py`; `docs/audits/calibration-bench-2026-07-11-panel-verdict.md` | Verdict Δ **0.00** 10/10; disagreement 0.0 (clear-cut) vs 2.0 (ambiguous); natural escalations **0/10** `[MEASURED]` |

---

## 4. Bảng slice 0001–0037

Nguồn: `## Status` của từng file `docs/issues/*.md` `[MEASURED]`, đối chiếu code `[INFERRED]`.

| Slice | Status theo doc | Đối chiếu | Evidence |
| --- | --- | --- | --- |
| 0001 Evaluate one answer | Closed | done | `evaluator.py`, `test_evaluator.py` |
| 0002 Skill-state update | Closed | done | `skill.py`, `test_skill.py` |
| 0003 weighted_score guard | Closed | done | `evaluator.py:742-802`, cross-check tests |
| 0004 Provider router + cutover rehearsal | Closed | done (MiMo rehearsal historical) | `llm.py` `LLMRouter`; audit `0015` |
| 0005 Micro-loop | Closed | done | `microloop.py`, 29 tests |
| 0006 Self-critique | Closed; "Continued by" → R-16 #71 | **done-then-superseded**: re-read độc lập đã bị 0027 thay bằng panel; trigger dormant | `evaluator.py:103-113, :838-844`; audit 07-27 shadow 0 |
| 0007 RAG + tool Interviewer | Closed | done | `interviewer.py`, `concepts.py`; audit `0007` |
| 0008 Concept-docs breadth | Closed | done | 40 notes; audits retrieval ×3 |
| 0009 Diagnostic Topic Plan + priors | Closed | done | `diagnostic.py`, 11 tests |
| 0010 Supervisor + LangGraph | Closed (0014 spun off) | done | `supervisor.py` |
| 0011 Study Planner + export | Closed | done | `study_planner.py`, `exporter.py` |
| 0012 UI + eval discipline | Closed; "Continued by" #61/#62/#63/#67 (all closed) | done | `web/`, `web_api.py`, `eval_harness.py` |
| 0013 Question-bank breadth | Closed (42 ≥ 40) | done; bank nay **45** (#94 forge merge) | `src/interview_coach/data/questions.yaml`; audit `question-bank-review` |
| 0014 Per-question failure isolation | Closed | done — chính là `except Exception` gây #119 | `supervisor.py:277-297` |
| 0015 Groq cutover + live validation | Closed | done (Groq nay chỉ là availability fallback) | audit `0015` |
| 0016 Web error visibility + reconnect | Closed | done | `web/e2e/reconnect-flow.spec.ts` (không chạy CI) |
| 0017 Web cancel lifecycle | Closed | done | `web_api.py:213-243` |
| 0018 Intent aborts | Closed | done | `microloop.py:39-57`, `supervisor.py:270-276` |
| 0019 Resume edge cases | Closed | done | `test_cli.py:279`, audit 0015 |
| 0020 Supervisor transport backstop | Closed | done | `supervisor.py:385-400` |
| 0021 Confidence-weighted evidence | Closed; "Continued by" ADR 0011 | done nhưng **inert** trên judge hiện tại (xem mục 5, 6) | `skill.py:75-88` |
| 0022 Bilingual judge bench | Closed; "Continued by": `english_delivery` n=5 < 8 vẫn ⚠ | done (35 cases, k=3) | `bench.py`, audit 07-27 |
| 0023 Longitudinal ledger | Closed; "Continued by" #65 (closed), 0035 (#83 open) | done (ledger); consumer chưa có | `ledger.py` |
| 0024 Bilingual interview mode | Closed; "Continued by" #78 (closed 08-04) | done | `language.py`, audit bilingual-mode |
| 0025 Question-pack platform | Closed; "Continued by" ADR 0014, #88 | done (contract); nửa taxonomy chưa | `bank.py:245-270`, `data/packs/fpt` |
| 0026 Rejection post-mortem | Closed | done | `postmortem.py`, 14 tests |
| 0027 Panel Verdict | Closed (2026-07-11); "Continued by" #71, ADR 0011 | done nhưng **0/10 natural escalations** | audit panel-verdict |
| 0028 Question Forge | Closed (2026-07-11); "Continued by" #88 | done; queue trống (3/3 merged #94) | `forge.py`, `data/forge/` |
| 0029 Replay bench | Closed (live-validated gpt-4o-mini) | done ở mức test; **không CLI**, 1 artifact, không pack (#113), không E1 | `replay.py`, `data/replay/` |
| 0030 Diagnostic transport backstop | Closed | done | `diagnostic.py:213-242` |
| 0031 VN scoring consistency | Closed (judge → gpt-5.4-mini) | done; residual mới = anchor holes (#96/#103) | audits gpt-5.4-mini, 07-27 |
| 0032 Seed wraparound | Closed | done | `seeds.py:34-36, :117-126` |
| 0033 Evidence-degrade confidence | Closed; "Continued by" #71 (haircut sau trigger) | done | `evaluator.py:364-389, :901-903` |
| 0034 Frontend redesign | **Không có `## Status` section** (vi phạm convention CLAUDE.md "Slice status lives in ## Status"); tracked #60 + PR #50 | **not landed** (PR #50 open, chưa rebase) | `web/src` không có Phudu/Be Vietnam Pro |
| 0035 Progress dashboard | Open (Wave 3) | not started | GH #83 |
| 0036 Accounts + Postgres | Open (Wave 3) | not started | GH #84 |
| 0037 CV/JD import | Open (Wave 3) | not started | GH #86 |

Không có slice doc cho: budget rails (R-25/#80, đã merge #118), per-role routing (#95), typed failover
(#98), LLM trace (#99), web hardening (#100), export persistence (#101), Docker (#102), ZenMux (#105),
mypy (#111), Chroma default (#112), retrieval labels freeze (#117) — các slice này chỉ có GH issue +
PR, không có `docs/issues/00NN` `[MEASURED]` listing `docs/issues/`.

---

## 5. Số liệu đã đo

Mỗi dòng một số, cite audit. Tất cả `[MEASURED]` trừ khi ghi khác.

### Judge bench (`coach bench`)

| Số | Giá trị | Nguồn |
| --- | --- | --- |
| Gate hiện tại | **35/35**, median-of-k k=3, temperature 0.2, `openai/gpt-5.4-mini` | `docs/audits/calibration-bench-2026-07-27.md` |
| Repeatability | 4 invocations liên tiếp × k=3 = 12 sweeps / 420 judgments, exit 0 cả 4, **0 straddle** | cùng audit, "DoD: repeatability" |
| Tokens một invocation k=3 | 192,136 (111 calls; 164,093 prompt / 28,043 completion) | cùng audit, "Token usage" |
| Tokens một sweep k=1 (29 case) | 41,802 (07-19); 41,806 (json-schema 07-11); 37,350 (trust-guards 07-11) | `calibration-bench-2026-07-19.md`; `-07-11-json-schema.md`; `-07-11-trust-guards.md` |
| Per-dimension bias 07-27 | communication −0.17 · correctness −0.26 · depth +0.03 · system_thinking +0.09 · mlops_awareness +0.25 (n=8) · english_delivery +0.00 (**n=5 < 8, ⚠ unstable**) | audit 07-27 |
| Weak/strong separation | mean weak 1.77 · mean strong 4.14 · gap 2.37 | audit 07-27 |
| Confidence calibration | [0.9,1.0]: n=34, mean 0.94, hit 100% · [0.7,0.9]: n=1, 0.89 | audit 07-27 |
| Shadow escalations | 0 tại ngưỡng <0.5, <0.6, <0.7 | audit 07-27; cũng 0 ở 07-19, trust-guards, json-schema |
| Trust guards fired 07-27 | 1 case (`panel_sd_retry_storm_en`, divergence 0.80, conf kept 0.97) | audit 07-27 |
| Transport noise 07-27 | `transport.backoff.openai` = 4 | audit 07-27 |
| Lịch sử gate | gpt-4o-mini 18/20 (07-07) → 19/20 (+prompt fix) → gpt-5.4-mini 20/20 (07-10) → 24/24 (bilingual) → 29/29 (panel, reanchor, trust, json-schema 07-11) → AMBER 28/28/29 of 29 (07-19) → 35/35 ×4 (07-27) | các audit tương ứng |
| Drift provider-side | communication bias +0.65 (07-10) → +0.34 (panel) → −0.10 (reanchor) → −0.28 (07-19) → −0.17 (07-27) cùng rubric text giữa 07-11 và 07-19 | audit 07-19 "Repeatability", 07-27 |
| Flake trước median-of-k | `dl_overfitting_weak_vi` 3.30/3.30/3.20 vs band top 3.2 (07-19); sau re-anchor 3.00/2.80/3.20 (07-27) | audit 07-19, 07-27 |
| Structural noise trước json_schema | flatten fold trên 8/29 case (32 relocations) | `-07-11-trust-guards.md` |
| Sau json_schema strict | 0 fold, 0 retry, 0 backoff / 29 | `-07-11-json-schema.md` |

### VN/EN fairness

| Số | Giá trị | Nguồn |
| --- | --- | --- |
| Paired |Δ| 07-27 | mean **0.17**, max **2.00** (`panel_sd_retry_storm` EN 1.00 / VN 3.00); 11/12 cặp Δ=0.00 | audit 07-27 |
| Per-dimension của cặp trên | depth EN 2.00 / VN 3.33 (Δ +1.33); system_thinking EN 1.33 / VN 3.67 (Δ +2.33); correctness +1.00; communication 0.00 | audit 07-27, GH #96 |
| Cặp `dl_overfitting_weak` | trước re-anchor correctness EN 2/2/2 vs VN 4/4/4 (label 3); sau: EN 3/3/3, VN 4/4/3; holistic |Δ| 1.10 → 0.00 | audit 07-27 |
| Lịch sử mean |Δ| | 0.30 (gpt-5.4-mini 07-10) → 0.25 (panel) → 0.00 (bilingual-mode) → 0.30 (07-19) → 0.17 (07-27) | các audit |
| Cross-model VN residual (trước gpt-5.4-mini) | `vnlp_segmentation_weak` EN 1.00 / VN 3.00 trên **cả** gpt-4o-mini và Groq llama-3.3-70b (|Δ| 2.00) | `-07-11-vn-consistency.md`, `-vn-consistency-groq.md` |
| Groq làm judge | 18/20 | `-vn-consistency-groq.md` |
| Toneless-VN gate vs bench | flip **0/35** bench cases | GH #78 comment 2026-08-01 (bench-nonregression test `tests/test_language.py:272`) |
| PR #104 (chưa merge) | max |Δ| 2.00 → 1.00; wording 2 đi 35/35, 35/35, **34/35**; wording 3 mới 1 invocation xanh | `gh pr view 104` (audit `-07-27-anchor96.md` chỉ trên nhánh PR) |

### Retrieval

| Số | Giá trị | Nguồn |
| --- | --- | --- |
| Toy in-memory (50 lookups) | 47/50 (94%) sau 4 label fix | `concept-retrieval-review-2026-07-11.md` |
| Chroma + bge-small-en-v1.5 | 46/50 · vietnamese_nlp 4/7 · vi self-retrieval 3/6 | `concept-retrieval-review-chroma-2026-07-11.md` |
| Chroma + multilingual-e5-small | **47/50** · vietnamese_nlp 6/7 · vi self-retrieval 4/6 · system_design 8/10 | `concept-retrieval-embedder-ab-2026-07-11.md` |
| Frozen eval set (post-#117) | 62 cases (59 seed + 3 live); in-memory floor **59/62** (95.2%), seed 56/59, live 3/3 — pinned trong test; **chưa có** số Chroma/e5 trên set 62 trong `docs/` | `tests/test_retrieval_eval.py:366-385` `[MEASURED]`; `data/bench/retrieval-labels.yaml` |
| Target set size | 150 (`TARGET_SET_SIZE`) — hiện 62 | `retrieval_eval.py:41` `[INFERRED]` |

### Replay / evidence semantics

| Số | Giá trị | Nguồn |
| --- | --- | --- |
| E2 n=1 | pre-R-24 mastery 0.38514 (|err| 0.18514) vs post 0.44257 (|err| 0.24257), **+31.0%** error, GT mlops 0.2 | GH #79 comment 2026-08-01 |
| Fold-every-turn pseudo-count | 1.7 / question tại conf 0.8, flat 1–8 turns; single-turn bit-identical trên 90,000 draws | GH #79 comment (branch chưa push) |
| Forced escalation | 10/10 verdict Δ 0.00; disagreement 0.0 / 1.0 / 2.0 phân tầng; 0/10 natural | `-07-11-panel-verdict.md` |
| confidence_weight range thực tế | conf min 0.89 (07-27) → weight 2·(0.25+0.75·0.89) = **1.84**; tối đa 2.0; range thiết kế [0.5, 2.0] | `[INFERRED]` `skill.py:75-88` + audit 07-27 (ADR 0002 ghi [1.93, 2.0] tại mean 0.95) |

### Content

| Số | Giá trị | Nguồn |
| --- | --- | --- |
| Bank built-in | 45 câu (ml_fund 9 / dl 9 / **mlops 12** / sd 9 / vnlp 6); 40 notes (34 en / 6 vi) | `[INFERRED]` đếm yaml; audit 07-11 ghi 42 trước #94 |
| Pack `fpt` | 20 câu (4/Skill), 10 notes | `[INFERRED]` đếm yaml |
| Forge live | 2/2 mlops drafts admitted (sim 0.17–0.20; strong 5.00 / weak 3.00); queue 3/3 merged | issue 0028 Status; `data/forge/review-queue-2026-07-11-report.md` |

### Ops / cost / latency

| Số | Giá trị | Nguồn |
| --- | --- | --- |
| Onboarding cold (#59) | **15m 01s** (uv sync 864s @ ~150 KB/s + 37s còn lại); warm 37s | GH #59 comment 2026-07-27 |
| Groq free tier | 98,352 / 100,000 TPD tiêu trong 1 phiên validation → 429 | audit `0015` |
| Reconnect e2e | 1.7 phút (live) | audit `0015` |
| Daily budget | 2,500,000 tokens (OpenAI free tier); 1 bench k=3 ≈ 7.7% | `usage.py:59` `[INFERRED]`; audit 07-27 tokens |
| Ledger local | 1,431 rows (2026-07-11 → 08-01); 1,380 `openai/gpt-5.4-mini`, **51 `mimo/test-model`** (11p/7c mỗi dòng); file gitignored | `[INFERRED]` `wc -l`, `grep -c` trên `logs/usage-ledger.jsonl` (local, không trên main) |
| Latency per call | **không có audit nào tổng hợp**; chỉ có trace `ms=` per call trong log | `llm.py:479-488` `[INFERRED]` |
| Token / Session end-to-end | **không có audit đo**; chỉ có sizing constants (mục 2) | `usage.py:94-109` `[INFERRED]` |
| Pricing hybrid routing | "$87 vs $176 / 1000 sessions" — con số trong ADR, không có artifact | `docs/adr/0010:17-18` (doc claim) |

---

## 6. Debt & workaround đang sống trên `main`

Liệt kê, không đánh giá. Tất cả `[INFERRED]` với file:line trừ khi ghi khác.

1. **`insufficient_quota` trong `question_node` → 1 `failed` zero-evidence** trước khi suspend (`supervisor.py:277-297`; `llm.py:442-449` tự ghi "about to be swallowed"). GH #119 Open (high). `[MEASURED]` #119: `rc=2 status=active stop_reasons=['failed']`.
2. **Replay artifact không mang pack** (`replay.py:148-155`); `replay_decision` gọi `decide_next_move` không bank (`:195`). GH #113.
3. **Rubric anchor holes**: `depth` và `system_thinking` không có 3 (`rubric.py:52-57, :67-75`; GH #96, PR #104 draft); `communication` không có 2 (`:58-66`; GH #103, ghi trong `KNOWN_SCALE_GAPS` `tests/test_bench.py`); **`mlops_awareness` chỉ có 1 và 5** (`:76-78`) — **không có issue nào**; `cases.yaml` anchors cũng không có mlops_awareness.
4. **Toneless-VN detector**: curated list 48 từ, còn `la` (`language.py:81`) và `du` (`:102`); comment "NOT collision-free" (`:49-55`); 2 strict xfail (`tests/test_language.py:191-199`); GH #78 đóng bằng distinct-count (#120) — đúng với memory "curated wordlist at its ceiling".
5. **Last-turn-wins** (`microloop.py:316`, docstring `:189-192`). R-24/#79 parked; E2 n=1 negative.
6. **Self-critique / panel dormant**: `SELF_CRITIQUE_CONFIDENCE_THRESHOLD=0.5` (`evaluator.py:572`) chưa bao giờ fire trên gpt-5.4-mini (shadow 0 ở 07-27 `[MEASURED]`); haircuts chạy sau trigger "on purpose" (`:901-903`) nên evidence-degrade cũng không trigger; `PanelBudget` 1/question (`:446-453`). GH #71 Open.
7. **`confidence_weight` gần như hằng** trên judge hiện tại (≥1.84, mục 5); ADR 0011 Proposed.
8. **MiMo remnants** dù endpoint chết 2026-06-03: `MimoClient` (`llm.py:594-616`); `primary_provider` **default `"mimo"`** (`config.py:69`) — không có `.env` thì `role_config("judge")` raise; fallback chain chứa mimo (`config.py:164`); `conftest.make_client` dựng router MiMo (`tests/conftest.py:167-171`) nên phần lớn `test_llm.py` chạy trên class MiMo; `test_mimo_can_disable_thinking_for_tool_loops` (`tests/test_llm.py:114-122`); comment rationale `interviewer.py:10-12, :34`; docstring `config.py:3-5`.
9. **Budget suspend rail nằm ngoài graph** ("Raised ONLY by the Session drivers", `usage.py:202-209`) — mỗi driver (`cli.py:366-409`, `web_api.py:956-985`) tự lặp lại logic; web gửi suspend dưới dạng `session_error` không có type riêng (`web_api.py:891-896`).
10. **Ledger đa tiến trình**: reader không lock, comment "needs real locking, not this" (`ledger.py:49-50`); single-worker guard là rào duy nhất (`web_api.py:353-384`).
11. **Exports không có retention** (`web_api.py:944-951`; chỉ checkpoint reaper 7 ngày `config.py:130`).
12. **Docker image không có rag extras** (`Dockerfile:27-42`) → trong container `concept_store=auto` rơi về in-memory với WARNING (`concepts.py:323-331`); tức deploy artifact chạy toy ranker, không phải Chroma+e5 đã A/B.
13. **`eval_harness` 5 case hard-coded** vẫn là admission gate của Forge (`forge.py:36, :425-445`) trong khi ADR 0009 bác nó làm gate judge.
14. **Bench và replay không nằm trong CI** (`ci.yml`); replay bench không có CLI; artifact replay duy nhất từ gpt-4o-mini 2026-07-07, trace thiếu `llm_calls` (pre-R-26).
15. **`forge.py:566-568`** report text nói failover "silently swaps the judge" — sai sau #95/#98.
16. **`Settings.fallback_provider`** `return "mimo"` unreachable (`config.py:167`); `LLMRouter.__init__` default fallback expr chỉ tests dùng (`llm.py:683`).
17. **Judge pin có 2 lối thoát**: `COACH_ALLOW_UNVALIDATED_JUDGE` (`config.py:99, :233`) và nhánh `RoleClients.single` khi `settings` không có `role_config` (`llm.py:970-974`).
18. **Product cap (480 questions/ngày) chỉ áp cho web** (`usage.py:521-538`, `web_api.py:774`), CLI không.
19. **Test fixture ghi vào ledger thật** khi `COACH_USAGE_LEDGER` không set: 51 dòng `mimo/test-model` trong `logs/usage-ledger.jsonl` local (file gitignored; quan sát local, không phải state của main).
20. **Serde golden không cover** raw SqliteSaver checkpoint bytes và web API payloads (`tests/test_serde_golden.py:45-52`).
21. **`SKILLS` hardcode ở 2 nơi** (`diagnostic.py:22-28`, `web/src/lib/types.ts:1-7`); unknown `target_role` → all PERIPHERAL im lặng (`diagnostic.py:323`). ADR 0014 Proposed.
22. **`english_delivery` bench n=5 < `BIAS_MIN_SAMPLES=8`** → bias flag ⚠ vĩnh viễn cho tới khi thêm 3 case (issue 0022 "Continued by").
23. **Retrieval eval set 62/150** target; chưa có số Chroma/e5 trên set frozen (`docs/` không có `/62`).
24. **PR #104 draft treo từ 2026-07-27** chờ 2 invocation xanh (~200k tokens/invocation); audit `-07-27-anchor96.md` + ADR 0009 (e)(f) chỉ trên nhánh PR. **PR #50** open chưa rebase, slice 0034 không có `## Status`.
25. **Nhánh local/remote đã merge chưa dọn** (mục 1).
26. **Study Planner không có input history**; coaching memory (0035/#83) chưa có consumer nào.
27. **Không có test ADR 0006** kiểu prompt-construction (addendum yêu cầu).
28. **`docs/issues/0022:45`** nói "20 hand-labelled cases (10 EN/VN pairs)… anchors for two dimensions" trong khi `:84` cùng file nói 35 — nội bộ mâu thuẫn.

---

## 7. Drift list — "doc nói X" vs "code làm Y"

| # | Doc nói | Code / thực tế | Nguồn |
| --- | --- | --- | --- |
| D1 | README `:114` "29-case calibration bench is green"; `:255` "green (29/29) … latest report `-07-11-reanchor.md`"; `:181` "passes coach bench 20/20 (issue 0031)" | 35 cases, 35/35 median-of-k k=3, audit mới nhất `-2026-07-27.md`; README không nhắc median-of-k | `README.md`; audit 07-27 |
| D2 | README `:143` "Low-confidence Evaluator judgments get exactly one Self-critique pass" | Không còn self-critique pass độc lập; escalation → panel; trigger 0 lần fire | `evaluator.py:911-936`; audit 07-27 |
| D3 | README `:145` "MiMo thinking disabled"; `:154` "`PRIMARY_PROVIDER=mimo\|groq`"; `:178-179` "`openai`, `groq`, or `mimo`"; `:284-286` `MimoClient` + `reasoning_content` | MiMo endpoint chết 2026-06-03; validated judge openai; README `:33-35` tự nói "MiMo is retired" (mâu thuẫn nội bộ) | `README.md`; audit 0015 |
| D4 | README `:321` "a Playwright demo-flow spec" | 2 spec (`demo-flow`, `reconnect-flow`) | `web/e2e/` |
| D5 | README không nhắc `postmortem`, `forge`, `bench --k`, `usage` | 4 subcommand tồn tại | `cli.py:960-1330` |
| D6 | CONTEXT.md "Self-critique: A re-evaluation the Evaluator triggers on itself when its own score has low confidence" | Cơ chế là panel (Skeptic/Advocate + re-read); trigger dormant | `evaluator.py`; audit 07-27 |
| D7 | CLAUDE.md "Slice status lives in the doc's `## Status` section" | 0034 không có `## Status` | `docs/issues/0034` |
| D8 | CLAUDE.md nhắc #78 như issue sống ("#78 parked") | #78 CLOSED 2026-08-04 bởi #120 | `gh issue view 78` |
| D9 | ADR 0001 amendment: "phrase-list validator (`supervisor.py:569`)" | Structured `will_probe_skill` validator (`supervisor.py:523-571`, #107) | code |
| D10 | ADR 0002 addendum: "Either outcome is recorded here with the numbers" | E2 datapoint (+31% error) chỉ trong GH #79 comment; ADR chưa sửa | GH #79 |
| D11 | ADR 0002 addendum cite `microloop.py:284` | nay `:316` | code |
| D12 | ADR 0006 addendum: "prompt-construction unit tests assert that no prior-session transcript text enters the message lists" | Không có test nào; chỉ docstring `ledger.py:7` | grep tests |
| D13 | ADR 0007: paired EN/VN là "regression proof that the same technical content scores the same" | max |Δ| 2.00 in-band trên `panel_sd_retry_storm`; #96/#103 Open | audit 07-27 |
| D14 | ADR 0008: "the engine ships content-free" | Bank 45 câu + 40 notes đóng gói trong `src/interview_coach/data/`; `SEED_QUESTIONS` là list ml_fundamentals | `bank.py:29-55`, `seeds.py:76` |
| D15 | ADR 0009(a) cite `llm.py:392`, `config.py:69`; ADR 0010 cite `config.py:56` | `llm.py:392` = constructor; `config.py:69` = default primary; `config.py:56` → `:101`; order ở `config.py:164` | code |
| D16 | ADR 0009(d) "~150k for k=3 over 35 cases" | Đo 192,136; preflight code 70k×3 = 210k | audit 07-27; `cli.py:735-738` |
| D17 | ADR 0009(b) label provenance từ "replayed live-session answers" | Không có code path; chỉ harvest retrieval queries | `retrieval_eval.py:358-486` |
| D18 | ADR 0009(c) two-bench contract (replay bench là gate cho Supervisor) | Replay bench không CLI, không trong CI, không đo pack, 1 artifact 07-07 | `replay.py`; #113 |
| D19 | ADR 0009 addenda (e) "unscoped anchor clause is a bar change", (f) "one green invocation is not a measurement" | Chỉ trên nhánh PR #104, chưa vào `docs/adr/0009` trên main | `gh pr view 104` |
| D20 | ADR 0010: "each role maps to its own provider/model and decoding"; "Defaults reproduce today's behavior exactly" | Không có bảng default per-role; unset role → primary + global temperature 0.2; hybrid chỉ là comment `.env.example` | `config.py:206-219` |
| D21 | ADR 0011 "over the 29 cases" | 35 cases | `data/bench/cases.yaml` |
| D22 | ADR 0005 addendum + CLAUDE.md: "never `failed`-and-advance" cho budget exhaustion | 1 `failed` item vẫn ghi khi quota chết trong câu hỏi | #119; `supervisor.py:277-297` |
| D23 | ADR 0003 "Why" + `interviewer.py:10-12` rationale MiMo | Addendum 2026-07-19 đã tuyên bố lịch sử; code comment và `MimoClient` chưa dọn | code |
| D24 | `.env.example:2` "the other configured providers are fallback" (số nhiều) | Đúng **một** fallback | `config.py:158-167` |
| D25 | `forge.py:566-568` "a mid-run provider failover silently swaps the judge" | Judge pinned, không failover | `llm.py:978` |
| D26 | `docs/issues/0022:45` "20 cases (10 pairs), anchors for two dimensions" | 35 cases, 12 pairs, 5 dimension có anchor | `data/bench/cases.yaml` |
| D27 | Audit `question-bank-review-2026-07-11` "42 questions (9/9/9/9/6)" và memory "bank 15→42" | 45 (mlops 12) sau #94 | `questions.yaml` |
| D28 | `docs/deploy.md` + Docker là "deploy artifact" chạy Chroma default (#112 "Chroma is the default concept store") | Image không cài rag extras → container chạy in-memory store | `Dockerfile:27-42`; `concepts.py:323-331` |
| D29 | `skill.py:8-11` docstring: correlations/priors "out of scope" | Correlations áp trong `diagnostic.py:354-362` | code |
| D30 | ADR 0009(a): judge failover = "retry-same-model, degrade, or suspend" | Sau 4 attempt, exception rơi vào `question_node` → `failed`-and-advance ("degrade"); không có suspend cho lỗi transport judge (chỉ có cho quota qua ledger latch ở node kế) | `llm.py:425-462, :916-919`; `usage.py:563-569` |

---

## Chưa đọc / chưa xác minh (ghi rõ thay vì đoán)

- Diff của PR #50 (frontend redesign) và nội dung nhánh `spike/*`, `worktree-wf_*` — chỉ đọc mô tả trong GH issue/PR.
- `docs/reference/MVP_v1_2day.md`, `MVP_v2.md` — archived, cố ý không đọc theo ràng buộc.
- Nhãn từng case trong `data/bench/cases.yaml` và `retrieval-labels.yaml` — không re-review nội dung nhãn, chỉ đếm và đối chiếu anchors.
- Thân từng test — chỉ đếm `def test_` và đọc tên/marker; con số coverage theo file, không có line coverage (`pytest-cov` có trong dev deps nhưng không chạy).
- `web/src` component internals ngoài các file inventory nêu; Playwright e2e không chạy.
- Các claim "verified 2026-07-27 / 08-01" trong `docs/deploy.md:146-175` — không chạy lại Docker/compose.
- Không chạy bất kỳ LLM live nào (`-m live`, `coach bench`, replay) — mọi số judge lấy từ artifact đã check-in.
- `pytest -m rag` (Chroma/e5) không chạy — không xác minh Chroma path trên máy này.
